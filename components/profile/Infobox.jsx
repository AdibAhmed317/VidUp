import { View, Text } from 'react-native';
import React from 'react';

const Infobox = ({ title, containerStyle, textStyle }) => {
  return (
    <View>
      <Text>Infobox</Text>
    </View>
  );
};

export default Infobox;
